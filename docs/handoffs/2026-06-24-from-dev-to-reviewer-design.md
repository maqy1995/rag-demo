# 交接单 — 概要设计 v1 (MAQ-8)

> multica-issue: [MAQ-8](mention://issue/5cfcacd2-1c88-4079-81a7-79a1abdef8ab)
> 起点：资深全栈开发工程师（`01386b69…`）
> 终点：审查员（Reviewer, `e57a9ea0…`）
> 日期：2026-06-24

## 交付物

- [x] [`docs/dev/design.md`](../dev/design.md) — 概要设计 v1（替换原占位三段式 + 待决项，扩为分层架构 + 契约 + 决策链 + 验收对照）
- [x] 本交接单 — 告诉审查员怎么在 30 分钟内确认 OK

## 验收口径

请审查员按下面顺序 5 步核对，全部通过即可在 issue 上挂一条 "approved with comments" / "approved"：

1. **范围核对**（5 分钟）：对照 PRD v0.3 目录，§3 F1–F12 + §8 验收点是否在 design.md 都有落地位置（§10 验收对照表 + §11 阻塞表）。
2. **决策链核对**（10 分钟）：§3.5 `answer` 决策链 + §3.4 `is_defined_in_hits` 纯函数签名是否对齐 PRD §8.2 v0.3 三档判定（US4 / US6 / GENERATED）。如 Reviewer NB1 的修复路径有偏差，请明确指出。
3. **接口对照**（5 分钟）：§3.6 端点表是否与 PRD §7.2 + §7.3 完全一致（SSE 帧顺序、错误响应格式、8 个端点）。
4. **横切核对**（5 分钟）：§6.1 config / §6.2 日志 / §6.3 错误三个 L4 模块是否符合 PRD §5（错误响应格式 / 日志格式）+ §6.2.3（`.env` 注入）。
5. **阻塞清单核对**（5 分钟）：§11.1 B1–B8 是否 owner / 截止齐全；§11.2 S1–S4 是否合理；B1–B5 是否按 §7.1 依赖图顺序串行。

> **特别关注点**（design.md 内的"非显然"取舍，欢迎挑战）：
> - §3.1 11 个模块目标行数（合计 ≤ 2k 行 Python + ≤ 600 行 HTML）—— 是否过紧 / 过松？
> - §3.4 把 `validate.py` 独立成模块而非塞进 `generate.py` —— 模块粒度 OK 吗？
> - §4.1 冷启动 demo 路径里 T+30s 边界的设计（即便 ingest 未完成，前端必须可点示例按钮）—— 是否符合 PRD §3 F8 + §8.2 原意？
> - §8.1 性能预算表新增了"增量更新 ≤ 30s"行 —— PRD 没明说，是否过度承诺？

## 已知风险 / 后续动作

| 风险 / 待办 | Owner | 触发条件 |
|------------|-------|---------|
| `data/index.sample/` 缺（S4 软阻塞） | 开发 | 任意 ADR 落地后 |
| `is_defined_in_hits` 正则仍为 PRD §8.2 初版 | 开发 | ADR-0001 拍板后替换 |
| §10 §8.3 3a 验收因缺产物未跑通 | 开发 | 等 S2 10 题样例 + S4 示例索引 |
| §11.1 B6/B7/B8 产品硬阻塞未答复 | **产品** | MAQ-5 close 前 |
| §11.2 S1/S2 产品软阻塞未答复 | **产品** | 对应 ADR 落地前 |
| §3.6 `POST /api/usage` 端点的 JSON schema | 开发 | 落地时起草（PRD §8.4 + handoff §开发待办提的埋点端点） |

## 联系人

- 起点：资深全栈开发工程师（`01386b69…`），GitHub: maqy1995
- 终点：Reviewer（`e57a9ea0…`）
- 项目：知识库问答（`7d66ac3d…`）

## 流程留痕

- 本轮 design.md 从原占位三段式骨架扩为分层架构 + 契约 + 决策链 + 验收对照，由 1 轮内完成。
- Reviewer 收到后请在 [MAQ-8](mention://issue/5cfcacd2-1c88-4079-81a7-79a1abdef8ab) 评论挂结论；若批准，开发侧即按 §7.1 顺序起草 ADR-0001 → 0003 → 0002 → 0004 → 0005。
- 若审查员有任何 §7.1 依赖图顺序、§3.1 模块粒度、§11 阻塞清单的异议，请直接在 design.md 顶部评论或在本交接单回执。