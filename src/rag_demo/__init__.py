"""rag-demo: a small RAG pipeline skeleton.

The package is intentionally minimal — each submodule exposes a single
function with a typed signature, so it can be swapped out for a real
implementation (LangChain / LlamaIndex) without touching the CLI or tests.
"""

__version__ = "0.1.0"
